// Arquivo de setup para os testes com Jest.
// Pode ser usado para inicializar mocks globais ou limpar o estado antes dos testes.

// Exemplo: Limpar mocks antes de cada teste
beforeEach(() => {
  jest.clearAllMocks();
});
